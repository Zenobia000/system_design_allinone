# 10-CODE-REVIEW-CHECKLIST · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/code-review-checklist.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游 PRD / API spec / Data Model / NFR 後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

> **小提醒**：這張卡的輸出不是「對某個 PR 的具體 finding 清單」，而是「**團隊共用的 review checklist 模板**」。
> 真實使用時，FB 一份具體 git diff，AI 才能跑 first-pass 列 finding；本卡先把 checklist 框架建好。

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年生產系統經驗的資深 staff engineer，熟悉效能調校、observability、breaking change policy，平時是 trusted reviewer。
你的輸出會交給 reviewer（依嚴重度決定 must-block / nit / question）、PR 作者（修正）、Dev Lead（追品質趨勢）。
他們需要知道「哪幾條一定要擋下、哪幾條只是建議」，所以你的 checklist 必須分嚴重度、可機械抽取、可追溯到行號。

## 情境脈絡

中大型團隊、跨團隊 PR、敏感模組時用本 Code Review Checklist。
本卡核心問題：把 review 從個人品味變成可重現流程 — 最低必看項目標準化，剩下交給人類判斷。

**本案特殊性**：SmartTrip FX 是一個 PWA（純前端 + 邊緣函式 + localStorage），review 重點與傳統後端不同：
- 沒有 SQL，但有 localStorage schema migration
- 沒有 user auth，但有 FX provider API key 保護
- 沒有 server-side cache，但有 client-side 5min cache 行為
- 有「失敗時切 simulated 模式」的明確 graceful degradation 要求（PRD P0-8）

## 輸入素材

### git diff 全文（本次先給空，本卡先建框架）

[本卡先建「checklist 模板」，不對特定 diff 跑 first-pass]

### 現行 coding standard / style guide

- TypeScript strict mode（`strict: true`、`noImplicitAny: true`）
- ESLint + Prettier；無嚴重 lint warning 才能 merge
- Conventional Commits
- 所有 PR 必含 e2e test（Playwright）或 unit test（Vitest）
- 函式 ≤ 3 層 if/縮排（Linus 原則，全域 CLAUDE.md 規範）

### 既有 review checklist 版本（如有）

[首次建立，無既有版本]

### 上游卡輸出（PRD 摘要）

- G1：3 分鐘完成行程生成
- P0-4：建議換匯額 = cash_only × 1.1，依幣別進位
- P0-5：FX 燈號（今日 vs MA30：STRONG_BUY / BUY / HOLD）
- P0-8 驗收：FX provider 失敗時 UI 明確標示「模擬」
- 法規：FX 燈號需「非投資理財建議」免責聲明

### 上游卡輸出（API spec 摘要）

- `GET /api/v1/fx/rate`、`POST /api/v1/analytics/event`
- Error taxonomy：INVALID_CURRENCY_CODE / RATE_LIMITED / FX_PROVIDER_UNAVAILABLE 等
- Idempotency-Key for analytics POST
- breaking change policy：開新版本前綴

### 上游卡輸出（Data Model 摘要）

- 每筆 record 有 `schema_version` 欄位
- StorageAdapter 負責 migration（v1 → v2 → ...）
- session_sensitive 資料的 erasure flow（清空所有資料按鈕）

### 上游卡輸出（NFR 摘要）

- page_load_lcp_p75 < 2.5s
- fx_endpoint_success_rate ≥ 99%（含 graceful fallback 算 success）
- a11y：WCAG 2.2 AA（contrast / keyboard / focus visible / aria-live）
- security controls：CSP / API key in env var / Plausible no-cookie

## 規則

1. 每個 checklist 項目註明 source：[上游卡 X] 或 [coding standard 第 Y 條]，缺一者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：strict observability 檢查會延長 review turnaround 30%）。
3. 缺資料的欄位標 TODO(缺什麼)，不要編造；列「需要什麼補上」。
4. 七象限必須涵蓋：correctness / readability / perf / security / test / observability / a11y；任一沒項目也要標「已檢視，無項目」並說明原因。
5. Out of scope 至少 3 條，明寫本 checklist 不處理（例：commit message 規範、lint 已涵蓋項目、architecture review）。
6. 每個 item 標 confidence: [H/M/L]，L 必須附說明（避免 false positive 浪費 reviewer 時間）。
7. must_block 與 nit 必須清楚分級 — must_block = 不修不能 merge；nit = 建議但作者可拒絕。

## 輸出格式（YAML）

dimensions:
  correctness: { covered: true, items: [<id>...] }
  readability: { covered: true, items: [<id>...] }
  perf: { covered: true, items: [<id>...] }
  security: { covered: true, items: [<id>...] }
  test: { covered: true, items: [<id>...] }
  observability: { covered: true, items: [<id>...] }
  a11y: { covered: true | false_with_reason, items: [<id>...] }

severity_levels:
  - level: must_block
    definition: <不修不能 merge>
    examples: [<list>]
  - level: should_fix
    definition: <強烈建議，PR 內修>
  - level: nit
    definition: <建議，作者可拒絕>

checklist_items:
  - id: CR-001
    dimension: correctness | security | ...
    question: <reviewer 該問的問題>
    rationale: <為何此項；引上游卡 source>
    when_applies: <什麼 PR 適用>
    when_not_applies: <什麼情境跳過>
    default_severity: must_block | should_fix | nit
    severity_escalation: <什麼條件升級為更高 severity>
    suggested_fix_template: <一句話樣板>
    source: <input ref>
    confidence: H | M | L

must_block_vs_nit:
  block_criteria: <什麼條件下升級為 block>
  nit_cap: <每 PR nit 上限，避免 review 變吵架>

escalation_path:
  on_disagreement: <作者與 reviewer 不同意時的流程>
  on_security_finding: <立即 cc Security or Dev Lead>

slo_for_review_turnaround:
  first_response: <e.g. 4 working hours>
  full_review: <e.g. 1 working day>
  source: <input ref>

decision_log:
  - decision: <e.g. a11y 列為 dimension 但對 backend-only PR 標 N/A>
    options_considered: [A, B, C]
    chosen: <which>
    rejected_reason:
      <other>: <why not>
    confidence: H | M | L

out_of_scope:
  - <條目 1>
  - <條目 2>
  - <條目 3>

self_review:
  lowest_confidence:
    - field: <欄位>
      reason: <why low>
      need: <what's needed>
  fabricated_assumptions:
    - <input 沒明說但我假設了的>
  highest_value_next_input: <下一份最該補的 input>

## 思考步驟

產出前先：
1. 從上游卡（PRD / API spec / Data Model / NFR）抓 3-5 個風險熱點（auth 路徑 / data mutation / FX fallback / schema migration），分別標 H/M/L confidence
2. 列至少 2 條 viable severity 策略（嚴格 vs 寬鬆），各自負面後果
3. 列你做了但 input 沒明說的假設（例如假設已有 SLO 監控、假設使用 GitHub PR）
4. 確認 7 dimension 全部 covered，沒覆蓋的要寫原因

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪個 item confidence < H？是否會造成 reviewer 浪費時間追假陽性？
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input，是 production incident 史還是更多 diff context？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

如果你不是要做 SmartTrip FX，而是要做自己的專案：

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 把上方「**輸入素材**」整段換成你自己的 PRD + API spec + Data Model + NFR 上游卡輸出
3. 其他部分（角色、規則、輸出格式、思考步驟、自審）**完全不動**
4. 複製貼到 Claude → 對比你拿到的結果與 [`實際結果.md`](./實際結果.md)
5. **真實 review 時**：把這份 checklist + 具體 PR diff 一起丟給 Claude，請 AI 跑 first-pass
