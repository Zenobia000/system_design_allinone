# 11-Unit Test · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/unit-test.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游卡（PRD P0-4「建議換匯額 = cash_only × 1.1」）後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年自動化測試經驗的資深 QA lead，熟悉 test pyramid、contract testing、chaos engineering、property-based testing、mutation testing (Stryker / PIT)。
你的輸出會交給 Dev（實作測試）、CI（執行）、reviewer（判斷覆蓋是否足夠）、未來 refactor 的人（當作安全網）。
他們需要知道「哪些 case 一定要測、哪些是 over-engineering」，所以你的 plan 必須機械可消費、邊界清楚、能對應到 spec。

## 情境脈絡

邏輯有分支、邊界條件、或會被其他模組依賴時用本 Unit Test。
本卡核心問題：讓重構不再靠賭 — 覆蓋率是副產品，可重構性才是目標。

## 輸入素材

### 函數簽名（TypeScript）

```typescript
// src/lib/fx/cash-suggestion.ts
type ItineraryItem = {
  id: string;
  title: string;
  estimatedCost: number;        // 該項花費（單位由 currency 決定）
  currency: 'JPY' | 'KRW' | 'THB' | 'USD' | 'TWD';
  paymentType: 'cash_only' | 'card_ok' | 'either';
};

type CashSuggestion = {
  currency: string;
  amount: number;               // 已加 10% 緩衝 + 進位後的建議換匯額
  twdApprox: number;            // 台幣約當
  breakdown: { cashOnlySum: number; bufferRate: number };
};

export function suggestCashAmount(
  items: ItineraryItem[],
  targetCurrency: 'JPY' | 'KRW' | 'THB' | 'USD',
  fxRateToTWD: number,
): CashSuggestion;
```

### 功能規格（來自 PRD P0-4 + 種子簡報）

- **規則**：建議換匯額 = `sum(items where paymentType === 'cash_only' AND currency === targetCurrency).estimatedCost` × 1.1
- **進位規則（依幣別）**：
  - JPY：無小數，向上進位到 100 日圓（例：12345 → 12400）
  - KRW：無小數，向上進位到 1000 韓元
  - THB：保留 2 位小數，向上進位
  - USD：保留 2 位小數，向上進位
- **acceptance criteria（PRD P0-4）**：顯示金額、台幣約當、緩衝說明；數字與規則一致
- **期望成果（種子簡報）**：事後實際現金花費與建議值誤差 < 15%
- **法規約束**：FX 燈號需「非投資理財建議」免責聲明（本函式不負責顯示，但測試需確認回傳結構不假裝是金融建議）

### 既有測試 + coverage 報告

```
TODO(尚未撰寫任何測試，coverage 為 0)
```

## 規則

1. 每個 test case 註明 source：[spec 第 X 段] 或 [signature 推導] 或 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：mock 太多會降測試價值，但不 mock 又會把 unit 變 integration、CI 變慢 5x）。
3. 缺資料的欄位標 TODO(缺什麼)，不要編造（例：spec 沒寫錯誤訊息文案就標 TODO，不要編一個）。
4. 必須涵蓋：happy path / boundary / error path / null & empty / concurrency（若 stateful）/ idempotency（若有副作用）。
5. Out of scope 至少 3 條，明寫不測什麼（例：不測 wiring code、不測 third-party library 內部、不做 UI snapshot test）。
6. 每個 test case 標 confidence: [H/M/L]，L 必須附說明（例：邊界值不確定就標 L 並列「需要 spec 確認」）。
7. mocking_strategy 必須說明「mock 什麼、不 mock 什麼、為何」 — 預設不 mock 同 module 內 pure function。

## 輸出格式（YAML + 範例測試碼）

先給 YAML test plan，再給 1–2 個關鍵 case 的 Vitest 範例碼（TypeScript）。

```yaml
targets:
  - kind: function
    name: <fully qualified name>
    signature: <type signature>
    public_api: true | false
    source: <input ref>

test_cases:
  - id: TC-001
    target: <ref to targets[].name>
    category: happy | boundary | error | null_empty | concurrency | idempotency
    input: <inline value 或 generator spec>
    expected_output: <value / exception type / side effect>
    edge_rationale: <為何是 edge>
    source: <input ref>
    confidence: H | M | L

mocking_strategy:
  mock: [<external IO / time / random / network>]
  do_not_mock: [<same-module pure function>, <value object>]
  rationale: <為何此邊界>
  source: <input ref>

coverage_target:
  line: <e.g. 80%>
  branch: <e.g. 75%>
  mutation_kill: <e.g. 60%>
  rationale: <為何此值>

performance_assertions:
  has_perf_test: true | false
  p99_latency_budget: <e.g. 10ms or N/A>
  rationale: <為何需要 / 不需要>

mutation_test_score:
  tool: Stryker | N/A
  target_score: <0-100>
  known_survivors: [<TODO(尚未跑過)>]

decision_log:
  - decision: <如：用 table-driven 還是 property-based>
    options_considered: [enumerated, property_based, fuzzing]
    chosen: <X>
    rejected_reason:
      <opt>: <why not>
    confidence: H | M | L

out_of_scope:
  - <條目 1>
  - <條目 2>
  - <條目 3>
```

## 思考步驟

產出前先：
1. 從 signature + spec 抓 3-5 個分支點（幣別 switch、進位規則、空陣列、cash_only 過濾），各標 H/M/L
2. 列至少 2 條 viable mocking 策略（純函式不 mock vs mock fxRateToTWD source）的負面後果
3. 列你做了但 input 沒明說的假設（例如：負數 estimatedCost 是否合法）
4. 確認 6 類 case（happy/boundary/error/null/concurrency/idempotency）都被檢視

## 輸出

（依 output_schema YAML 填寫 + 附 1–2 個 Vitest 範例碼）

## 自審

1. 哪個 test case confidence < H？是因為 spec 模糊還是 signature 不明？
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input，是更完整的 spec 還是 production error log？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

如果你不是要做 SmartTrip FX，而是要做自己的專案：

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 把「**輸入素材 → 函數簽名**」換成你自己要測的函式（從 PR 抓或從 `03-prd` 的 P0 需求推導）
3. 把「**輸入素材 → 功能規格**」換成對應的 acceptance criteria（從 `04-acceptance-criteria/` 來）
4. 其他部分（角色、規則、輸出格式、思考步驟、自審）**完全不動**
5. 複製貼到 Claude → 對比你拿到的結果與 [`實際結果.md`](./實際結果.md)
