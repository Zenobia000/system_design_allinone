# 06-C4-DIAGRAM · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/c4-diagram.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游 PRD / ADR 後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 10+ 年分散式系統經驗的資深 software architect，熟悉 C4 model（Context / Container / Component / Code）、ADR、DDD、CAP、event-driven、microservices trade-off。
你的輸出會交給 stakeholder（看 Level 1）、工程團隊（看 Level 2）、新人 onboarding（看 Level 2/3）、code reviewer（看 Level 3/4）。
每層必須對應一種讀者，混層 = 義大利麵。所以圖描述必須結構嚴格、分層乾淨、技術選型與 ADR 一致。

## 情境脈絡

跨系統整合、團隊 ≥ 10 人、有外部 stakeholder 需要溝通架構時用本 C4。
本卡核心問題：用四層抽象讓每種角色看到「他需要看的那層」，避免每次溝通都重畫新圖。

## 輸入素材

### Codebase 結構 / 主要模組 / package 邊界

SmartTrip FX 是一個 Next.js 14 App Router 的純前端 SPA，無自有後端服務。主要 package 邊界：

- `app/` — Next.js 路由（`/`, `/start`, `/result`, `/my-trips`, `/expense/[id]`）
- `components/` — UI 元件（TripForm、TierCard、ItineraryTimeline、FXBadge、ExpenseList）
- `lib/fx/` — FX 燈號計算（今日 vs MA30 偏離 %）
- `lib/budget/` — 三方案產生器（Low/Mid/High）與建議換匯額（cash_only × 1.1）
- `lib/storage/` — localStorage adapter（Trip / Expense / FXRateCache）
- `lib/analytics/` — 事件發送（generate / save_trip / add_expense）
- 外部 API：FX rate provider（P0-8 待接入，目前為模擬資料）

### 既有 ADR 清單與技術選型

- ADR-001：採用 Next.js 14 App Router（SSG + ISR）— 來自 PRD 主要約束「免登入、即開即用」
- ADR-002：MVP 不做後端，行程資料存 localStorage — 來自 PRD Non-Goals「不做帳號/雲端同步」
- ADR-003：FX 即時匯率走 server route handler 代理（避免 client 暴露 API key），快取 5 分鐘
- ADR-004：分析事件選 Plausible（無 cookie、輕量、符合 PRD 隱私定位）
- 部署：Vercel（已上線 <https://smarttrip-fx.vercel.app>）

### Stakeholder 清單與外部系統

- 主要 actor：台灣出境旅客（25–40 歲，種子簡報 目標受眾）
- 外部系統：FX rate provider（候選 ExchangeRate-API / Open Exchange Rates）、Plausible Analytics、Vercel CDN/Edge
- 不接：訂房/訂票/支付（PRD Non-Goals）

### 上游卡輸出（PRD 摘要）

- G1：3 分鐘內生出含建議換匯額的行程，誤差 < 15%
- P0-8（關鍵缺口）：接入即時匯率取代模擬值
- P0-9（關鍵缺口）：基本使用分析
- Non-Goals：不做帳號、不做訂房、不做多人分帳

## 規則

1. 每個 element 註明 source：[input 第 X 段] 或 [ADR-XXX]；無法歸因者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例：拆 microservice 則犧牲跨服務 transaction 的簡潔性）。
3. 缺資料的欄位標 TODO(缺什麼)，不要編造模組或外部系統。
4. 分層紀律：Level 1 只放 system + actor + external；Level 2 才放 container；Level 3 才放 component；任何混層必須在 omitted_details 解釋。
5. Out of scope 至少 3 條（例：Level 4 / code 層 default 不畫；deployment topology 走另張 deployment diagram；資料模型走 data-model 卡）。
6. 每個關鍵宣稱標 confidence: [H/M/L]，L 必須附說明。
7. 至少標 3 個架構風險點（單點故障 / cyclic dependency / 跨 boundary chatty call）。

## 輸出格式（YAML + 內嵌 Mermaid C4）

level:
  current: context | container | component | code
  audience: <stakeholder | engineer | code reviewer>
  rationale: <為何畫這層>

elements:
  - id: E-1
    name: <e.g. SmartTrip Web>
    type: system | container | component | actor | external
    responsibility: <一句話>
    tech: <e.g. Next.js 14 / TypeScript> | n/a
    source: <input ref / ADR ref>
    confidence: H | M | L

relationships:
  - from: E-1
    to: E-2
    purpose: <e.g. fetch today's FX rate>
    protocol: <REST / fetch / postMessage / localStorage API>
    sync_async: sync | async
    confidence: H | M | L

boundary_definitions:
  - name: <e.g. SmartTrip System>
    contains: [E-1, E-3]
    rationale: <DDD 邊界依據>

omitted_details:
  - what: <e.g. CDN / Edge cache>
    why: <不在本層的讀者關心範圍>

notation:
  syntax: Mermaid C4
  diagram_context: |
    <Level 1 Mermaid inline>
  diagram_container: |
    <Level 2 Mermaid inline>
  diagram_component: |
    <Level 3 Mermaid inline>

architecture_risks:
  - risk: <e.g. FX provider 單點失效>
    severity: H | M | L
    follow_up: <e.g. ADR-XXX 評估雙來源 fallback>

decision_log:
  - decision: <e.g. 為何不畫 Level 4 code 層>
    options_considered: [A, B, C]
    chosen: A
    rejected_reason:
      B: <why not>
      C: <why not>
    confidence: H | M | L

out_of_scope:
  - Level 4 (code) 預設不畫，只在 reviewer 需要時補
  - Deployment / infra topology 由 deployment diagram 處理
  - 資料模型 / ERD 由 data-model 卡處理

## 思考步驟

產出前先：
1. 從 codebase 抓 3-5 個核心 container / bounded context，標 H/M/L confidence
2. 列至少 2 條 viable 邊界路徑（依 team 拆 vs 依 BC 拆），各自負面後果
3. 列你做了但 input 沒明說的假設（例：假設 IdP 是 external、假設 cache 是 sidecar）
4. 確認分層紀律 — 沒有 component 偷渡進 context 層

## 輸出

（依 output_schema YAML 填寫，並在 notation 區提供 Mermaid 圖）

## 自審

1. 哪個 element / relationship confidence < H？列出來與所需補充資料。
2. 哪些邊界假設來自我而非 input / ADR？標出來。
3. 如果只能再追加一份 input，是哪一份？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

如果你不是要做 SmartTrip FX，而是要做自己的專案：

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 把上方「**輸入素材**」整段換成你自己的 codebase 結構 + ADR 清單 + Stakeholder 清單
3. 若沒有正式 ADR，把 `03-prd/` 與 `05-adr/` 的卡輸出貼上去當代替
4. 其他部分（角色、規則、輸出格式、思考步驟、自審）**完全不動**
5. 複製貼到 Claude → 對比你拿到的結果與 [`實際結果.md`](./實際結果.md)
