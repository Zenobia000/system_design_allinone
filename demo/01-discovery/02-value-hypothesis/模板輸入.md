# 02-Value-Hypothesis · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/value-hypothesis.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游 [`01-jtbd/實際結果.md`](../01-jtbd/實際結果.md) 後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 5+ 年 agile 經驗的資深 PO，熟悉 lean product、INVEST 原則、JTBD、backlog 拆解、A/B testing、leading indicator 設計、kill criteria 文化。
你的輸出會交給 PM（寫 PRD）、Dev Lead（評估成本）、QA（設計驗證指標），他們會用來決定要不要把這 idea 排進 sprint。
所以假設必須可證偽、實驗成本必須 < 1 週、kill criteria 必須誠實到敢真的執行。

## 情境脈絡

新功能進 backlog 前、估時 > 1 個 sprint、不確定使用者買不買單時用本 deliverable。
本卡核心問題：把「我覺得有用」翻成可驗證的假設，避免把高成本工程資源花在沒人在乎的功能。

## 任務

根據以下 input 產出「價值假設卡」draft。要驗證的核心 idea 是：**「以『金錢決策』為主軸的旅遊規劃工具（先算錢、再排行程），會比現有以景點為主軸的工具更被衝動型旅客採用」**。

## 輸入素材

### 模糊 idea / 老闆說要做的功能描述

SmartTrip FX 一句話定位：「唯一會幫你算『該帶多少外幣現金、何時換最划算』的個人旅遊小工具」。
我們相信「先算錢」這個切角是差異化護城河，但目前沒有任何外部證據，純屬團隊內部信念。
本卡要驗證的不是某個功能，而是**整個 MVP 的定位假設是否成立**：4 週驗證窗結束時，我們敢不敢繼續做 V1。

### discovery 資料：persona / JTBD / journey pain

**目標受眾（取自種子簡報）**：
25–40 歲台灣衝動型出境旅客，一年 1–4 趟、以日韓泰為主、預算敏感、習慣帶現金、決策快怕麻煩。次要族群：第一次自由行的新手。

**待解痛點（取自種子簡報）**：
不知道該帶多少外幣現金、哪些地方只收現金、現在換匯划不划算。換太多 → 回國再換虧匯差；換太少 → 現場狼狽。在一趟 NT$40,000 的旅程中，匯損與多換的成本可達數百到上千元。現有行程規劃工具與 OTA 完全不處理這件事，旅客只能憑感覺換匯。

**上游 JTBD 卡（取自 01-jtbd/實際結果.md，列出與本卡最相關的兩條）**：

- JTBD-001：When 我下週要出國但不知道帶多少外幣現金, I want to 一鍵算出該換多少 + 看到換匯時機燈號, so I can 不換太多虧匯差也不會當場狼狽。
  - success_criteria: ≤ 3 分鐘完成估算 / 現金誤差 < 15% / 不需要切換 3 個工具
  - opportunity_size: 1–4 次/年/人，P1 + P2
- JTBD-003：When 我想出國但選擇困難時, I want to 用心情 + 預算範圍快速生出三種行程選項, so I can 不用花一週爬文就能拍板。
  - success_criteria: 3 方案皆 ≤ 預算上限 / 心情/主題對得起來

### 現有 metric baseline（conversion / retention / activation）

⚠️ **本專案無任何 baseline**：產品 v0.1 剛上線，未接入分析工具（種子簡報 主要約束「P0-9 基本使用分析」待補）。
所以下列假設的 baseline 必須誠實標 `confidence: L` 並標 `TODO(從首兩週實測校準)`。

### 主要約束（取自種子簡報）

- 不做帳號雲端同步（MVP 用 localStorage）
- 不做訂房比價（外部連結導出）
- **4 週 MVP 驗證窗** ← 本卡的決策時間盒
- FX 燈號需「非投資理財建議」免責聲明

### 種子簡報已預設的成功指標（要驗證、不是要照抄）

- 啟用率 ≥ 40%
- 存檔率 ≥ 25%
- 30 天回訪 ≥ 20%
- 換匯誤差 < 15% 的行程比例 ≥ 60%

⚠️ 這些數字是團隊拍腦袋，**沒有 baseline 校準**。本卡要把它們轉成可被殺的 kill criteria，而非當作既定目標。

## 規則

1. 每個假設必須註明 source：[input 第 X 段]；無依據者標 [來源未明示，需確認] 並降 confidence。
2. Trade-off 必須列負面後果（驗證 hypothesis A，則犧牲 hypothesis B 的 sprint 時間 / 學習機會）。
3. 缺資料的欄位標 TODO(缺什麼)，不編造 baseline；列「需要什麼補上」。
4. 實驗成本控制：test_method 必須 < 1 週且優先選 no-code / mock；超過須在 decision_log 說明為何。
5. Out of scope：明列 3 條本文件不處理（例如：實作細節、UI 設計、定價策略）。
6. 每個 assumption 標 confidence: [H/M/L]，L 必須附說明為何不確定。
7. Kill criteria 必須可被觸發（不能寫「不管結果都繼續」）；違反此規則須在 decision_log 註明。

## 輸出格式（YAML）

customer_segment:
  required: true
  type: string
  ref: <persona id>
  source: <input ref>
  confidence: H | M | L

problem_statement:
  type: string
  format: "<segment> struggles to <job> because <pain>"
  source: <input ref>

proposed_value:
  hypothesis: "我們相信 <行為改變> 對 <使用者> 有價值，因為 <理由>。我們會用 <指標> 驗證。"
  behavior_change: <observable behavior>
  leading_indicator: <metric measurable within 2 weeks>
  source: <input ref>

riskiest_assumption:
  required: true
  type: string
  example: "使用者真的願意每天打開 dashboard，而非只在月底"
  category: enum[desirability, viability, feasibility]
  confidence: H | M | L
  why_risky: <why this kills the idea if wrong>

test_method:
  type: enum[fake_door, concierge, wizard_of_oz, prototype_test, smoke_test, painted_door]
  cost_estimate: <≤ 1 week>
  no_code: bool
  participants: int
  rationale: <why this method validates the riskiest assumption>

success_threshold:
  metric: <e.g., click-through rate>
  baseline: <current value>
  target: <value that confirms hypothesis>
  source: <input ref>
  confidence: H | M | L

kill_criteria:
  required: true
  type: list[string]
  triggerable: true  # must be executable, not aspirational

decision_log:
  - decision: <test_method 選擇>
    options_considered: [fake_door, concierge, prototype]
    chosen: fake_door
    rejected_reason:
      concierge: <為何不選>
      prototype: <為何不選>
    confidence: H | M | L

out_of_scope:
  - 不設計實作細節（屬 PRD / ADR）
  - 不畫 UI mockup（屬 UX design）
  - 不定價或商業模式（屬 monetization brief）

## 思考步驟

產出前先 step-by-step：
1. 從 input 抓 3-5 個 assumption，分 desirability / viability / feasibility，標 H/M/L
2. 找最 riskiest 的那條（錯了就整個 idea 死）
3. 列至少 2 條 test_method 路徑與各自負面後果（cost vs signal strength）
4. 列你做了但 input 沒明說的假設（baseline / 母體大小）
5. 確認 kill criteria 真的可被觸發

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪個 assumption 的 confidence < H？需要什麼補資料？
2. 哪些 baseline / target 來自我的腦補而非 input？標出來。
3. 如果只能再追加一份 input（用戶訪談 / analytics baseline / 競品 benchmark），哪一份對輸出品質提升最大？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

如果你不是要做 SmartTrip FX，而是要做自己的專案：

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 跑完 [`01-jtbd/`](../01-jtbd/) 拿到你自己的 JTBD 卡輸出
3. 把上方「**輸入素材**」整段替換成你自己的 brief + JTBD 卡
4. 其他部分（角色、規則、輸出格式、思考步驟、自審）**完全不動**
5. 複製貼到 Claude → 對比你拿到的結果與 [`實際結果.md`](./實際結果.md)
