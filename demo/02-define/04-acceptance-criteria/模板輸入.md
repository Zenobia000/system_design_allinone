# 04-Acceptance-Criteria · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/acceptance-criteria.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游 [`03-prd/實際結果.md`](../03-prd/實際結果.md) 後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 40 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年自動化測試經驗的資深 QA lead，熟悉 test pyramid、contract testing、chaos engineering、WCAG 2.2 a11y、OWASP top 10。
你的輸出會交給 Dev（自測 + TDD）、QA（寫 e2e / contract test）、PO（sprint review 驗收）、Audit（合規留痕）。
他們需要直接把你的 Given/When/Then 轉成自動化測試碼，所以條件必須機械可驗證、無主觀字眼。

## 情境脈絡

每個 user story 進 sprint 前必備；每個 epic gate review 前必檢。
本卡核心問題：讓「做完了」這句話有客觀證據，誰看都一樣，不靠主觀感受。

## 任務

根據以下 input 產出「Acceptance Criteria · 驗收條件」draft，含 happy path + edge case + NFR + security 限制。
本卡聚焦 **FR-002（建議換匯現金額）+ FR-003（FX 換匯燈號）+ FR-004（即時匯率資料源）** 三條 P0 需求（這是 SmartTrip FX 護城河的核心三件套）。

## 輸入素材

### User story（取自 03-prd FR-002/003/004 + JTBD-001）

**作為**衝動型出境旅客（25–40 歲、預算敏感、習慣帶現金），
**我想要**在 3 分鐘內看到「該換多少外幣現金 + 現在換划不划算」，
**這樣我**就不會換太多虧匯差、也不會換太少當場狼狽。
（來源：01-jtbd JTBD-001 statement）

### PRD section（取自 03-prd/實際結果.md）

**FR-002 建議換匯現金額**：
- 建議額 = 該方案 cash_only 行程總和 × 1.1（10% 緩衝）
- 依目的地幣別進位（JPY 進位至 1000、KRW 進位至 10000）
- priority: P0, confidence: H, source: [種子簡報 待解痛點, JTBD-001]

**FR-003 FX 換匯燈號**：
- 燈號邏輯：今日匯率 vs MA30 偏離 %（STRONG_BUY / BUY / HOLD）
- 畫面附「非投資理財建議」免責聲明
- priority: P0, confidence: H, source: [種子簡報 主要約束, 期望成果]

**FR-004 即時匯率資料源**：
- 燈號與匯率來自真實 API
- API 失敗時降級到快取，畫面標「模擬」
- priority: P0, confidence: H, source: [種子簡報 P0-8]

**NFR 摘錄**：
- latency：行程生成端到端 p95 < 3 秒；FX 燈號讀取 p95 < 500ms
- a11y：WCAG 2.2 AA（戶外通勤手機使用，對比度與可觸控區域）
- security：data_classification: internal；主要威脅 XSS + 第三方匯率 API 偽造
- audit：分析事件 90 天無 PII；FX 燈號須符合金管會「非投資理財建議」標示

**合規要求**：
FR-003 顯示燈號時須包含「非投資理財建議」免責聲明（種子簡報 主要約束 法規）。

### 既有 test data / mock / 第三方 sandbox 清單

- 既有 mock 匯率資料：`fixtures/fx-rates-mock.json`（5 種主要幣別 × 90 天歷史）
- 第三方 API 候選：exchangerate.host（免費 tier，1000 req/月）/ OpenExchangeRates（付費）
- Sandbox：兩家都有 sandbox endpoint，但無 SLA 保證
- ⚠️ Plausible 分析事件（FR-005）為內部埋點，不屬本卡範圍

## 規則

1. 每條 criterion 註明 source：[input 第 X 段]；無法歸因者標 [來源未明示，需確認]。
2. 必須 Given/When/Then 三段，無主觀字眼（禁止「順、好用、合理」）；若用主觀字眼負面後果是 QA 無法寫測試、永遠扯皮。
3. Edge cases 至少 3 條，必須涵蓋：空值 / 超大值 / 並發 / 權限不足 / 網路斷線 / 第三方逾時 至少 3 類。
4. Error handling 必須明定：error code / 使用者訊息 / log 級別 / retry policy。
5. NFR 必須涵蓋 performance threshold（p95 latency / throughput）+ a11y（WCAG 2.2 level）+ security（input 驗證 / authZ / data classification）+ audit（log retention）。
6. 標 confidence: [H/M/L]；L 必須附「需要什麼資料補上」。
7. Out of scope 至少 3 條（例：跨 story 整合測試走 e2e suite、效能壓測走 perf budget、安全滲透測試走 security review）。

## 輸出格式（YAML）

criteria:
  - id: AC-001
    type: enum[happy_path, edge_case, error_path, nfr_perf, nfr_a11y, nfr_security, nfr_audit]
    given: <初始狀態 + 資料 + 環境>
    when: <觸發動作 + 輸入>
    then: <可驗證的結果 + 資料變化 + 副作用>
    mockable: bool
    third_party_dependencies: [<API / sandbox name>]
    source: <input ref>
    confidence: H | M | L

happy_path:
  count: <≥ 2>
  ac_ids: [<AC-id>]

edge_cases:
  count: <≥ 3>
  categories_covered: [empty, oversize, concurrency, perm_denied, network_failure, third_party_timeout]
  ac_ids: [<AC-id>]

error_handling:
  - trigger: <例：使用者輸入無效 email>
    error_code: <例：VAL-001>
    user_message: <i18n key + 範例文案>
    log_level: enum[debug, info, warn, error]
    retry_policy: enum[none, immediate, exponential_backoff]
    source: <input ref>

performance_thresholds:
  p50_latency: <ms + 量測點>
  p95_latency: <ms + 量測點>
  throughput: <rps>
  source: <PRD NFR ref>

security_constraints:
  input_validation: [<規則>]
  authz_rules: [<who can do what>]
  data_classification: enum[public, internal, confidential, restricted]
  audit_log_fields: [<必記錄欄位>]
  audit_retention: <時間 + 合規依據>

decision_log:
  - decision: <例：為何 edge case A 拆成兩條而非合併>
    options_considered: [<A>, <B>, <C>]
    chosen: <A>
    rejected_reason:
      B: <為何不選>
      C: <為何不選>
    confidence: H | M | L

out_of_scope:
  - <本卡不含 thing 1>
  - <本卡不含 thing 2>
  - <本卡不含 thing 3>

self_review:
  lowest_confidence:
    - field: <欄位>
      reason: <為什麼 confidence 低>
      need: <需要什麼補上>
  fabricated_assumptions:
    - <我推測但 input 沒明說的>
  highest_value_next_input: <下一份最該補的 input>

## 思考步驟

產出前先：
1. 從 input 抓 3-5 個關鍵 signal（user story 的 benefit 對應的可量測訊號 / NFR 數字 / 合規條款），標 H/M/L confidence
2. 列至少 2 條 viable AC 切法（按 user flow 切 vs 按 system response 切），各自負面後果
3. 列你做了但 input 沒明說的假設（既有 mock 可用、第三方 sandbox 穩定、log infra 已備）
4. 確認每條 AC 都能被自動化測試碼直接消費
5. 每條 P0 需求至少 1 個 happy_path + 1 個 error_path + 1 個 nfr 型 AC

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪條 AC 含主觀字眼或無法機械驗證？列出來與改寫建議。
2. 哪些假設來自我而非 input（特別是 mock / 第三方 sandbox）？標出來。
3. 如果只能再追加一份 input（NFR 數字 / 合規條款全文 / 既有 mock 清單），哪一份對 AC 品質提升最大？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

如果你不是要做 SmartTrip FX，而是要做自己的專案：

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 跑完 [`03-prd/`](../03-prd/) 拿到你自己的 PRD（含 FR + NFR）
3. 把上方「**輸入素材**」整段替換成你自己的 user story + PRD section
4. 其他部分（角色、規則、輸出格式、思考步驟、自審）**完全不動**
5. 複製貼到 Claude → 對比你拿到的結果與 [`實際結果.md`](./實際結果.md)
