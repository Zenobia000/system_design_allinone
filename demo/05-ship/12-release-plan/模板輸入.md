# 12-Release Plan · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/release-plan.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游卡（PRD P0-8 即時匯率 + P0-9 分析、`11-unit-test` 通過）後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 5+ 年 agile 經驗的資深 PO，協作 7+ 年 SRE 經驗的 release engineer，熟悉 backlog 拆解、user story 寫作、INVEST 原則、SLO/SLI/error budget、incident response、postmortem 文化、Conventional Commits、SemVer。
你的輸出會交給 Dev Lead（確認依賴）、QA（執行驗證）、SRE on-call（值班待命）、Customer Success（對外通知）、PM（決策）。
他們需要在上線前 24 小時讀完並各自就位，所以你的 plan 必須含具體時間軸、責任人、退路連結。

## 情境脈絡

Release 涉及 schema、外部承諾、跨團隊或灰度時必要。
本卡核心問題：把上線從「按 deploy」變成「有對齊、有證據、有退路」的協作文件，避免每次靠 Slack 即興指揮。

## 輸入素材

### 專案脈絡（種子簡報）

SmartTrip FX 為 4 週 MVP 驗證窗的個人專案。目標受眾為 25–40 歲台灣衝動型出境旅客，目前無付費用戶、無正式 SLA、無正式 on-call 輪值（單人 maintainer = 我自己）。
無正式 customer 對外承諾，但 vercel 部署 URL（<https://smarttrip-fx.vercel.app>）已開始接受有機流量。

### 本次 release scope（PRD v0.1 缺口收尾）

| # | 變更 | 類型 | PR |
|---|---|---|---|
| 1 | P0-8：把模擬匯率換成真實匯率（exchangerate.host） | feat(fx) | #42 |
| 2 | P0-9：接入 Plausible analytics，事件：generate / tier_select / save_trip / add_expense / deal_link_click / return_visit | feat(analytics) | #43 |
| 3 | FX 燈號顯示「資料來源 + 最後更新時間」（法務合規：非投資理財建議） | feat(fx) | #44 |
| 4 | localStorage schema 加 `version: 1` 欄位（為 V1 雲端同步預留） | refactor(storage) | #45 |
| 5 | suggestCashAmount 單元測試（見 `11-unit-test/`），coverage 95% line / 70% mutation | test(fx) | #46 |

### Commit 訊息（Conventional Commits）

```
feat(fx): integrate exchangerate.host as real FX source (closes P0-8)
feat(analytics): wire Plausible events for 6 KPI signals (closes P0-9)
feat(fx): show data source + last-updated timestamp on lamp (compliance)
refactor(storage): add schema version field for forward compat
test(fx): add suggestCashAmount unit test suite (95% line, 70% mutation)
```

### 服務依賴圖

- **上游依賴**：
  - exchangerate.host（免費 tier，1000 req/month，無 SLA）
  - Plausible Cloud（付費 $9/月，99.9% uptime SLO）
  - Vercel Hosting（Hobby tier）
- **下游 consumer**：
  - 目前無 API consumer
  - 有機流量使用者（無正式承諾）

### 既有測試結果

- `11-unit-test` 的 10 個 case 全綠
- 手動煙霧測試：能在 Chrome / Safari 完成「生成 → 儲存 → 記帳」流程

## 規則

1. 每個結論註明 source：[input 第 X 段]；無法歸因者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：合併兩個 release 省 ops 成本，但放大 blast radius）。
3. 缺資料的欄位標 TODO(缺什麼)，不要編造；列「需要什麼補上」。
4. SLO 與外部承諾必須涵蓋；任何 breaking change 必須附 deprecation 窗口與 customer comms。
5. Out of scope：明列 3 條本文件不處理（例如：detailed rollout commands、SLO 重新校準、行銷文案）。
6. 每個關鍵宣稱標 confidence: [H/M/L]，L 必須附說明為何不確定。
7. comms_plan 必須含 internal / external / support 三層，每層有 channel + audience + timing。
8. **必須含明確的 go/no-go 條件與 abort criteria，並指明誰做決策**。

## 輸出格式（YAML）

release_id:
  version: <SemVer>
  code_name: <optional>
  target_date: <ISO 8601 + TZ>
  source: <input ref>

scope_commits:
  - commit_sha: <short>
    type: feat | fix | perf | refactor | BREAKING
    summary: <one-line>
    pr_link: <URL>
    user_facing: true | false
    source: <input ref>
    confidence: H | M | L

risk_summary:
  high: [<risk + likelihood + mitigation>]
  medium: [<risk>]
  unknowns: [<unknown 1>, <unknown 2>, <unknown 3>]

dependency_window:
  upstream_freezes: [<service + window>]
  downstream_notifications: [<service + lead_time>]
  blackout_periods: [<例如：月結、行銷活動>]

go_no_go:
  decision_maker: <role>
  go_criteria: [<list 含具體門檻>]
  no_go_criteria: [<list 含具體門檻>]
  decision_time: <ISO + TZ>

comms_plan:
  internal: [...]
  external: [...]
  support: [...]

rollback_link: <URL to 13-rollback-plan or TODO>

post_release_metrics:
  slo_watch_window: <minutes>
  business_kpi_watch: [<metric + threshold>]
  success_criteria: [<criteria>]
  abort_criteria: [<criteria>]
  confidence: H | M | L

decision_log:
  - decision: <例：灰度 vs 全量>
    options_considered: [...]
    chosen: <X>
    rejected_reason: {...}
    confidence: H | M | L

out_of_scope:
  - <條目 1>
  - <條目 2>
  - <條目 3>

self_review:
  lowest_confidence: [...]
  fabricated_assumptions: [...]
  highest_value_next_input: <string>

## 思考步驟

產出前先：
1. 從 input 抓 3-5 個關鍵 signal（最大的 BREAKING change、最緊的依賴、最敏感的外部承諾）
2. 列至少 2 條 viable release 路徑（一次全量 vs 灰度 vs 分批），各自負面後果
3. 列你做了但 input 沒明說的假設（例如：customer 對 downtime 容忍度）
4. 確認 comms 三層都有人接

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪個欄位 confidence < H？列出來與所需補充資料。
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input，是哪一份？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

1. 跑 [`/start/`](http://localhost:3000/start/) 問卷 → 下載 `project-brief.md`
2. 把「**本次 release scope**」換成你自己的 PR 清單（`git log v0.1.0..HEAD --oneline` 抓出來）
3. 把「**服務依賴圖**」換成你自己的 upstream / downstream（沒有就誠實寫「無正式 consumer」）
4. 其他部分**完全不動**
5. 複製貼到 Claude → 對比 [`實際結果.md`](./實際結果.md)
