# 13-Rollback Plan · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/rollback-plan.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游卡（`12-release-plan` 的 abort criteria）後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年 SRE 經驗的資深 release engineer，熟悉 SLO/SLI/error budget、incident response、postmortem 文化、schema migration 反向化、blue/green、feature flag。
你的輸出會交給 on-call SRE（半夜 3 點按按鈕）、Dev Lead（確認資料兼容）、DBA（執行反向 migration）、PO（對外溝通）。
他們需要在 5 分鐘內判斷「按或不按」，所以你的 plan 必須步驟可逆、觸發明確、不可逆點預警。

## 情境脈絡

任何含 schema、契約、或不可逆操作的 release 必須有 rollback plan。
本卡核心問題：在 incident 發生前先決定「什麼指標觸發 → 誰按 → 按完會發生什麼 → 多久確認」，把猶豫成本降到 0。

## 輸入素材

### 對應 release plan（上游）

來自 `12-release-plan` v0.2.0：
- PR #42: 整合 exchangerate.host 取代模擬匯率
- PR #43: Plausible analytics 接入
- PR #44: FX 燈號顯示資料來源（合規）
- PR #45: localStorage schema 加 `version: 1` 欄位（**這是 schema migration**）
- PR #46: suggestCashAmount unit test

來自 `12-release-plan` 的 `abort_criteria`：
- TRIGGER-1: 上線後 30 分鐘內，FX API 失敗率 > 50%
- TRIGGER-2: 上線後 1 小時內，使用者回報「既存行程消失」≥ 1 件
- TRIGGER-3: Vercel deploy log 出現 unhandled exception 連續 5 次

### Deploy steps（Vercel）

```
1. git push origin main → Vercel auto-deploy production
2. Vercel build (約 2 分鐘) → 產生新 deployment URL
3. Vercel 自動把 production alias (smarttrip-fx.vercel.app) 指向新 deployment
4. 上線完成
```

### Migration scripts

PR #45 的 `migrate()` 函式：
```typescript
// src/lib/storage/migrate.ts
function migrate(stored: unknown): TripStorageV1 {
  if (!stored) return { version: 1, trips: [] };
  if (isV0(stored)) {
    // V0 -> V1: 加 version 欄位、把 expense.amount 從 number 改為 { value, currency }
    return { version: 1, trips: stored.trips.map(upgradeTrip) };
  }
  return stored as TripStorageV1;
}
```
**注意**：V0 → V1 後，舊版 SmartTrip FX (v0.1.x) 讀不回 V1 schema（無 backward read）。

### SLO 與業務指標

- 無正式 SLO（個人 MVP 專案）
- 業務指標：FX 燈號渲染成功率 ≥ 95%、localStorage migration 失敗 = 0
- MTTR 目標：≤ 15 分鐘（個人專案，能接受短暫不可用）

## 規則

1. 每個結論註明 source：[input 第 X 段]；無法歸因者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：選自動 rollback 會犧牲 X 分鐘 false-positive 風險）。
3. 缺資料的欄位標 TODO(缺什麼)，不要編造；列「需要什麼補上」。
4. SLO / data integrity 必須涵蓋；任何不可逆操作（drop column、destructive migration）必須單獨標 irreversible: true 並附 forward-only 補救方案。
5. Out of scope：明列 3 條本文件不處理（例如：DR drill、跨 region failover、application-level circuit breaker）。
6. 每個關鍵宣稱標 confidence: [H/M/L]，L 必須附說明為何不確定。
7. Rollback steps 必須 ordered，每步附 reversible: true/false 與預估耗時。
8. **每個 step 必須含可貼上執行的具體指令（Vercel CLI / git command / 瀏覽器點擊路徑），不能寫『redeploy previous』這種無法直接執行的句子**。

## 輸出格式（YAML）

triggers:
  TRIGGER-1: { signal: <SLI>, threshold: <%>, window: <minutes>, source: <ref> }
  TRIGGER-2: ...
  TRIGGER-3: ...

rollback_steps:
  - step: 1
    action: <具體動作>
    command: <可貼上的指令>
    reversible: true | false
    estimated_duration: <minutes>
    owner: <role>
    verification: <如何確認此步成功>
    source: <input ref>

data_compat_strategy:
  direction: forward | backward | both
  approach: <expand-contract | dual-write | shadow read>
  irreversible_points: [<point + 補救方案>]
  confidence: H | M | L

inverse_migrations:
  - migration_id: <fwd>
    inverse_script: <path | TODO(缺什麼) | irreversible>
    data_loss_risk: none | partial | total
    source: <input ref>

safe_window:
  timezone: <TZ>
  duration: <minutes>
  blackout_periods: [...]

abort_criteria:
  hard_stop: [...]
  soft_stop: [...]

comms_plan:
  internal: [...]
  external: [...]
  decision_maker: <role>

mttr_target: <minutes>

decision_log:
  - decision: <如：自動 vs 手動觸發>
    options_considered: [...]
    chosen: <X>
    rejected_reason: {...}
    confidence: H | M | L

out_of_scope:
  - <條目 1>
  - <條目 2>
  - <條目 3>

self_review:
  lowest_confidence: [...]
  fabricated_assumptions: [...]
  highest_value_next_input: <string>

## 思考步驟

產出前先：
1. 從 input 抓 3-5 個關鍵 signal（最危險的 migration、最緊的 SLO、blast radius 最大的 step）
2. 列至少 2 條 viable rollback 路徑（forward-fix vs full revert），各自負面後果
3. 列你做了但 input 沒明說的假設（例如：DB 是否支援 online schema change）
4. 確認所有不可逆點都有標記

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪個欄位 confidence < H？列出來與所需補充資料。
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input，是哪一份？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

1. 把上游 `12-release-plan/實際結果.md` 的 `abort_criteria` 整段貼到「對應 release plan」
2. 把「**Deploy steps**」換成你的部署流程（不是 Vercel 就改成你自己的 CI/CD 步驟）
3. 把「**Migration scripts**」換成你的 schema 變更（沒有的話寫「無 schema 變更」）
4. 其他部分**完全不動**
5. 複製貼到 Claude → 對比 [`實際結果.md`](./實際結果.md)
