# 15-Postmortem · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/postmortem.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游卡（`14-runbook` ALERT-FX-1 被觸發的假設情境）後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年 SRE 經驗的資深 SRE，熟悉 SLO/SLI/error budget、incident response、Google SRE blameless postmortem 文化、5 Whys / Fishbone 根因分析。
你的輸出會交給 服務 owner（補實作細節）、Architect（評估設計缺口）、Engineering Manager（排 action item 優先級）、Training Lead（補訓練）。
他們需要可執行的改善項，不是檢討個人，所以你必須 blameless 且每個 action 有 owner、due、severity。

## 情境脈絡

SEV-1/2 後 5 工作日內、或重複 SEV-3 出現時用本卡。
本卡核心問題：把「誰的錯」改寫成「系統的哪個缺口」，每次留下 ≤ 5 個可執行 action item。

## 輸入素材

### Incident Report

**Incident ID**: INC-2026-0530-01
**Severity**: SEV-2
**Service**: SmartTrip FX
**Affected**: 約 200 名有機流量使用者（依 Vercel Analytics 估算）
**Duration**: 12 小時（2026-05-30 02:00 ~ 14:00 UTC+8）

**Summary**：FX 換匯燈號連續 12 小時顯示「模擬資料」badge 而非真實匯率，但顯示給使用者的數字是 exchangerate.host 在 2026-05-29 23:50 拉到的快照（已過期 ~14 小時）。約 200 名使用者基於此資料做了換匯決策，預估影響金額：人均誤差 < 0.5%（外幣本身波動小），但**「燈號顯示模擬卻仍呈現過期真實數字」的不一致違反了 PRD P0-5 與合規承諾**。

**Impact**：
- 200 名使用者看到失準資料
- 違反 release plan 的 success_criteria 第 1 條（FX 燈號渲染成功率 ≥ 95%）
- 無 PII 洩漏、無資料丟失
- 商業影響：無付費用戶，但對「金錢優先」差異化定位的信任造成可量化傷害（GitHub 收到 3 個 issue）

### Chat log（事故當下，個人 Obsidian 筆記）

```
[02:00] exchangerate.host status 紅燈（社群推特看到）
[02:30] 我在睡覺，沒人值班，沒被 page（個人專案無告警）
[09:15] 起床，咖啡，看 GitHub —— issue #87 「燈號好像怪怪的」
[09:20] 開啟 Plausible，看到 fx_api_fail 從 02:00 開始累積 ~150 次
[09:22] curl exchangerate.host，回 503
[09:25] 應該執行 R1（fallback to mock）—— 但客戶端 code 已經 fallback 了
[09:30] 才發現「fallback 到 mock」邏輯有 bug：
        UI 顯示 mock badge，但 fx 數字仍從 localStorage 讀「上次成功的快照」
        即從 23:50 拉到的舊資料
        違反 PRD P0-5「失敗時明確標示模擬」—— 標了 badge 但數字不是 mock 該有的
[09:35] 提 hotfix PR #92：fallback 到 mock 時數字也用 mock
[10:15] PR 寫完，但發現自己沒有對應的 unit test → 補測試
[12:00] PR review（自己 review 自己）
[13:30] 合併並 deploy
[14:00] verify：UI 燈號 + 數字一致顯示 mock
[14:10] 在 GitHub 開 incident issue 公開時間軸
```

### Metric / Dashboard 截圖摘要

- exchangerate.host: 02:00 ~ 14:00 持續 503，14:30 恢復
- Plausible `fx_api_fail` 事件：12 小時內 ~3,200 次
- Plausible `generate` 事件：12 小時內 ~200 次（受影響使用者數來源）
- Vercel runtime log: 無 unhandled exception（fallback 邏輯沒丟錯，是邏輯本身有問題）

### Runbook 執行紀錄（如有偏離）

`14-runbook/` ALERT-FX-1 的 R1 在實作上有 bug：
- 設計意圖：fallback to mock = UI badge 切 mock + 數字也切 mock
- 實際行為：UI badge 切 mock + 數字仍用 localStorage 快照
- 結果：runbook 假設 R1 已是 safe default，但實作不符合假設

## 規則

1. 每個結論註明 source：[input 第 X 段]；無法歸因者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：選「加更多告警」會增加 alert fatigue X%）。
3. 缺資料寫 TODO(缺什麼)，不要編造；列「需要什麼補上」。
4. SLO compliance：列出本事故對 error budget 的消耗 %；NFR 含告警延遲、MTTR、MTTA。
5. Out of scope：明列 3 條本文件不處理（例如：個人績效檢討、商業影響細算、合約罰款計算）。
6. 每個關鍵宣稱標 confidence: [H/M/L]，L 必須附說明。
7. **Blameless：寫流程/工具/訓練/設計缺口，不寫人名作為根因；個人錯誤一律歸因於「系統未保護該操作」**。
8. **5 Whys 必須終止於系統性原因（process / training / monitoring / design），不能停在「人沒注意到」**。

## 輸出格式（YAML）

incident_ref:
  required: true
  source: <input ref>

timeline:                   # 從 chat log 抽出，加時間戳
  - { time: <ISO>, event: <string>, source: <ref> }

impact:
  users_affected: <number>
  duration_minutes: <number>
  slo_burn: <% or N/A + reason>
  business_impact: <string>

root_cause:
  five_whys:
    - why_1: <observed symptom>
    - why_2: <cause of symptom>
    - why_3: <cause of cause>
    - why_4: <systemic>
    - why_5: <organisational/process>
  primary_cause_category: enum[code, config, capacity, dependency, process, training, monitoring]
  source: <input ref>
  confidence: H | M | L

contributing_factors:
  - factor: <string>
    category: enum[detection, response, recovery, prevention]
    source: <input ref>
    confidence: H | M | L

what_went_well: [<至少 2 條>]
what_went_wrong: [<至少 2 條>]

action_items:
  - id: AI-001
    statement: <action>
    owner: <name/team or TODO>
    due: <ISO date or TODO>
    severity: enum[P0, P1, P2]
    category: enum[detection, response, recovery, prevention]
    success_criteria: <how we'll know it's done>
    tracking_link: <URL or TODO>
    source: <input ref>
    confidence: H | M | L

blameless_summary:
  type: string  # ≤ 200 字，給高層讀，不含人名

prevention_categories:
  detection: [<改善 1>, <改善 2>]
  response: [<改善 1>]
  recovery: [<改善 1>]
  prevention: [<改善 1>]

decision_log:
  - decision: <例：根因歸類為 monitoring vs code>
    options_considered: [...]
    chosen: <X>
    rejected_reason: {...}
    confidence: H | M | L

out_of_scope:
  - <條目 1>
  - <條目 2>
  - <條目 3>

self_review:
  lowest_confidence: [...]
  fabricated_assumptions: [...]
  highest_value_next_input: <string>

## 思考步驟

產出前先：
1. 從 input 抓 3-5 個關鍵 signal（first alert 延遲、ack 延遲、mitigation 選錯、rollback 缺）各標 H/M/L confidence
2. 列至少 2 條根因路徑（technical vs process）與各自的負面後果
3. 列你做了但 input 沒明說的假設
4. 確認 prevention 4 類（detection/response/recovery/prevention）都涵蓋

## 輸出

（依 output_schema YAML 填寫）

## 自審

1. 哪個欄位 confidence < H？列出來與所需補充資料。
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input（例如 deploy log、DB slow query log），是哪一份？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

1. 把「**Incident Report**」換成你自己的真實事故（沒有就不要寫 postmortem —— 這是 reactive deliverable）
2. 把「**Chat log**」換成你 Slack / Obsidian 的當下記錄
3. 把「**Runbook 執行紀錄**」換成 oncall 實際做了什麼（與設計的 runbook 哪裡偏離）
4. 其他部分**完全不動**
5. 複製貼到 Claude → 對比 [`實際結果.md`](./實際結果.md)
