# 14-Runbook · 模板輸入（複製貼到 Claude / ChatGPT）

> **這個檔案的用途**：把 `product_to_launch/content/deliverables/runbook.md` 的 `prompt-full` 套上 [SmartTrip FX 種子簡報](../../種子簡報.md) + 上游卡（`12-release-plan` post_release_metrics、`13-rollback-plan` triggers）後的成品 prompt。
>
> **怎麼用（小白 step-by-step）**：
> 1. 打開 Claude（或 ChatGPT/Gemini 任一）
> 2. 把下方「═══」之間的整段文字**全部複製貼上**
> 3. 按送出，等 30 秒
> 4. 拿到回應後跟 [`實際結果.md`](./實際結果.md) 對比

---

══════════════ ↓ 從這裡開始複製 ↓ ══════════════

## 角色

你是有 7+ 年 SRE 經驗的資深 on-call SRE，熟悉 SLO/SLI、incident response、Google SRE runbook 規範、chaos engineering、game day。
你的輸出會交給 凌晨三點被 page 的工程師（照做不思考）、新入職的 on-call（學習用）、自動化團隊（轉成 self-healing 候選）。
他們可能是非本服務 owner，所以每一步必須具體可貼上執行、每個 remediation 必須有 rollback。

## 情境脈絡

任何 paging alert、新服務上線前都要有 runbook。
本卡核心問題：讓被 page 的人不需要理解設計也能正確處理告警，並把處理步驟轉成可自動化候選。

## 輸入素材

### 服務架構摘要

SmartTrip FX 是一個 client-side 為主的 SPA：
- 前端：Next.js 14 部署於 Vercel（Hobby tier）
- 資料儲存：使用者瀏覽器 localStorage（無後端 DB）
- 外部依賴：
  - exchangerate.host（即時匯率 API，1000 req/month）
  - Plausible Cloud（analytics，付費 $9/月）
  - Vercel Edge Cache（1 小時 TTL）
- 無正式 SLO；個人 maintainer MTTR 目標 ≤ 15 分鐘

### 已知告警 / 異常清單（本次 runbook 要覆蓋的 4 條）

來自 `12-release-plan` 的 risk_summary + `13-rollback-plan` 的 triggers：

1. **ALERT-FX-1**：FX API（exchangerate.host）失敗率 > 50% 連續 30 分鐘
2. **ALERT-STORAGE-1**：localStorage 寫入失敗（quota exceeded 或 disabled）
3. **ALERT-DEPLOY-1**：Vercel deployment failed（build error 或 runtime exception）
4. **ALERT-ANALYTICS-1**：Plausible 事件 ingestion 失敗（24h 無事件進來但有有機流量）

### 過往 postmortem 與 chat log 摘錄

```
TODO(MVP 階段尚無正式 incident；本 runbook 為 preemptive 撰寫)
```

### Dashboard 連結與面板

- Vercel Dashboard: <https://vercel.com/sunny/smarttrip-fx>
- Plausible Dashboard: <https://plausible.io/smarttrip-fx.vercel.app>
- exchangerate.host status: <https://status.exchangerate.host>
- GitHub Issues (incident log): <https://github.com/sunny/smarttrip-fx/issues?label=incident>

## 規則

1. 每個結論註明 source：[input 第 X 段]；無法歸因者標 [來源未明示，需確認]。
2. Trade-off 必須列負面後果（例如：選 quick restart 會丟 in-flight request X%）。
3. 缺資料寫 TODO(缺什麼)，不要編造；診斷指令缺則寫 TODO，不要瞎猜命令語法。
4. SLO compliance：每個 remediation 標對 SLO 的影響（保護 / 燃燒 / 中性）；NFR 含執行時長、權限要求。
5. Out of scope：明列 3 條（例如：根因分析、長期設計改動、跨服務協調）。
6. 每個關鍵宣稱標 confidence: [H/M/L]，L 必須附說明。
7. **每個 remediation step 必須附 rollback 指令；不能只有「執行 X」沒有「如果 X 失敗執行 Y」**。
8. **每個 runbook entry 必須能在不打電話問 service owner 的情況下執行完畢**（否則該條退回補資料）。

## 輸出格式（YAML，4 個 entry 各填一份）

對每個 ALERT 各產出一份完整 runbook entry：

```yaml
- alert_name: <string>
  symptom:
    user_visible: <string>
    internal_signal: <metric / log pattern>
    source: <input ref>
    confidence: H | M | L

  diagnosis_steps:
    - step: <description>
      command: <exact command or query>
      expected_output: <what good looks like>
      if_anomalous: <next step pointer>
      source: <input ref>

  remediation_steps:
    - step_id: R1
      action: <description>
      command: <exact command>
      rollback: <exact rollback command — required>
      slo_impact: protect | burn | neutral
      time_estimate: <e.g. 2-5 min>
      permissions: <required role/IAM>
      source: <input ref>
      confidence: H | M | L

  escalation_criteria:
    - condition: <e.g. R1+R2 仍紅燈 > 10 min>
      escalate_to: <role / TODO(填上 on-call)>
      expected_ack: <time>

  related_dashboards: [...]

  last_tested:
    date: <ISO or TODO(需要排 game day)>
    result: pass | fail | not_tested

  on_call_specific_notes: [...]

  decision_log:
    - decision: <如：mitigation 選 restart vs failover>
      options_considered: [...]
      chosen: <X>
      rejected_reason: {...}
      confidence: H | M | L

  out_of_scope:
    - <條目 1>
    - <條目 2>
    - <條目 3>
```

## 思考步驟

產出前先：
1. 從 input 抓 3-5 個關鍵 signal（最常見 symptom、最快 mitigation、最常見誤判）各標 H/M/L
2. 列至少 2 條 mitigation 路徑（保守 vs 激進）與各自的負面後果
3. 列你做了但 input 沒明說的假設（如權限假設、依賴假設）
4. 確認每個 remediation 都有 rollback

## 輸出

對 4 個 ALERT 各產出一份 runbook entry（YAML 陣列）。

## 自審

1. 哪個欄位 confidence < H？列出來與所需補充資料。
2. 哪些假設來自我而非 input？標出來。
3. 如果只能再追加一份 input（例如最近 3 次同告警 chat log），是哪一份？為什麼？

══════════════ ↑ 複製到這裡為止 ↑ ══════════════

---

## 替換成你自己專案的方法

1. 把「**服務架構摘要**」換成你自己的 stack（不是 Vercel 就改成你的 hosting）
2. 把「**已知告警清單**」換成你自己 release-plan 與 rollback-plan 衍生的 trigger
3. 上游若有真的 postmortem（`15-postmortem/`），把摘錄貼到「**過往 postmortem 與 chat log 摘錄**」
4. 其他部分**完全不動**
5. 複製貼到 Claude → 對比 [`實際結果.md`](./實際結果.md)
